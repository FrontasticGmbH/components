import React from 'react';

import { ProductDetails } from 'components';

const ProductDetailsTastic = ({ data, stream }) => {
  return <ProductDetails data={data} stream={stream} />;
};

export default ProductDetailsTastic;
