import React from 'react';

import { HorizontalSpacer } from 'components';

const HorizontalSpacerTastic = ({ data }) => {
  return <HorizontalSpacer data={data} />;
};

export default HorizontalSpacerTastic;
